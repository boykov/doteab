(el-get 'sync (append
	       eab/package-sources-minimal

	       eab/el-get-sources-minimal
	       ))

(el-get 'sync '(
		;; ace-jump-buffer
		eev-current
		ace-window
		deferred
		ctable
		epc
		))
